<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>SAGE</display-name>

    <!--
        At startup the container parses every class in every listed jar, so only the jar that needs it is listed.
        An unlisted jar's web fragment and container initializer are ignored. spring-web's initializer is what starts
        the app, and a library added later that registers itself the same way has to be listed too.
    -->
    <absolute-ordering>
        <name>spring_web</name>
    </absolute-ordering>

    <context-param>
        <param-name>spring.profiles.active</param-name>
        <param-value>dev</param-value>
    </context-param>

    <mime-mapping>
        <extension>ico</extension>
        <mime-type>image/x-icon</mime-type>
    </mime-mapping>

    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/views/404.jsp</location>
    </error-page>

</web-app>